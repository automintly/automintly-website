/*
 * Automintly conversion tracking configuration.
 *
 * Tracking is intentionally disabled for launch. To activate it later:
 * 1. Set enabled to true.
 * 2. Choose "ga4" or "plausible" as the provider.
 * 3. Add the matching measurementId or domain.
 *
 * Never put passwords, API keys, form values, or customer data in this file.
 */
window.AUTOMINTLY_TRACKING_CONFIG = Object.freeze({
  enabled: false,
  provider: "none",
  measurementId: "",
  domain: "automintly.com",
  debug: false
});
